{
  "private": true,
  "scripts": {
    "release": "workspace-version",
    "test": "yarn wsrun test --exclude example-webpack-plugin"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "pipo-scripts": "^0.1.0",
    "workspace-release": "^0.4.3",
    "wsrun": "^1.3.0"
  }
}
